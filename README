To contribute:

1. Clone the repository: git clone git://github.com/jfernandez/newrelic-safari-extension.git
1. Enable the Develop menu in the Safari advanced preferences.
2. Open the Extension Builder from the Develop menu option.
3. Click on the + icon in the lower left. Select 'Add Extension' and the 'NewRelic.safariextension' folder.
4. Reload the extension from the Extension Builder window after modifying the source code.

* I modified the backbone.js source code to work with XML instead of JSON.
* To run the test suite open the SpecRunner.html file (test-js directory) in any browser.

To learn more about Safari extensions, check out the Safari Extension Developer Guide:
http://developer.apple.com/library/safari/#documentation/Tools/Conceptual/SafariExtensionGuide/Introduction/Introduction.html